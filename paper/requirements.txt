# Pinned dependencies for the deposited analysis scripts.
# Versions are those used to produce the figures and tables in the manuscript.
#
#   pip install -r paper/requirements.txt
#   python3 paper/scripts/run_all.py            # regenerate
#   python3 paper/scripts/run_all.py --check    # verify reported values only

rdkit==2026.03.4
numpy==2.4.6
matplotlib==3.11.1
scipy==1.17.1
pandas==3.0.5
